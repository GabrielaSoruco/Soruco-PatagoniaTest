server:
  port: 8001

spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/GabrielaSoruco/Soruco-PatagoniaTest
          searchPath: config-data
 application:
    name: config-service

